Repo for my personal website.

TODO, from most to least important:
- Organize the file structure, create subdirectories for each page in `/pages`
- Google analytics
- Better font/spacing
- Dark theme toggle
- Would like to be able to reuse components such as the navbar while only defining them once. Probably the best way to do that is using some front-end framework

Fixed issues:
- Formatting issue where `<p>` and `<ul>` that don't span multiple lines have smaller font size on mobile (fixed by adding some mobile formatting meta-parameters)